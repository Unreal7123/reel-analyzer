fastapi>=0.111.0
uvicorn[standard]>=0.29.0
playwright>=1.44.0
httpx>=0.27.0
pydantic>=2.7.1

# Optional NLP — spaCy 3.8+ is compatible with typer>=0.12
# Comment out if you don't need improved NLP accuracy
spacy>=3.8.0

# After install run: python -m spacy download en_core_web_sm