# ── Core API framework ────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.1

# ── Scraping tier 1: Playwright (Chromium) ───────────────────────────────────
# After pip install, run:  python -m playwright install chromium
playwright>=1.44.0

# ── Scraping tier 2: curl-cffi (Chrome TLS fingerprint) ─────────────────────
# Binary wheels available on PyPI for Linux x86_64, macOS, Windows
curl-cffi>=0.7.0

# ── Scraping tier 3 + tier 2 fallback: httpx ─────────────────────────────────
httpx>=0.27.0

# ── Optional: NLP accuracy boost with spaCy ──────────────────────────────────
# Comment out if you don't need it (saves ~200 MB of Docker image)
# After pip install, run:  python -m spacy download en_core_web_sm
spacy>=3.8.0
